---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: counter-pvc
  labels:
    type: local
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Mi
  storageClassName: default
